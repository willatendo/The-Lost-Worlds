package lostworlds.core;

//@Mixin(WorldGenSettings.class)
public class DimensionGeneratorSettingsMixin {
//	@Inject(method = "<init>(JZZLnet/minecraft/core/Registry;Ljava/util/Optional;)V", at = @At(value = "RETURN"))
//	private void seedGetter(long seed, boolean generateFeatures, boolean bonusChest, Registry<LevelStem> registry, Optional<String> string, CallbackInfo ci) {
//		WorldSeedHolder.setSeed(seed);
//	}
}
